{
  "name": "lambda-s3-object-image-processor",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "dependencies": {
    "axios": "^1.13.2"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.952.0",
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^25.0.2",
    "esbuild": "^0.27.1",
    "sharp": "^0.34.5"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node24 --outdir=dist && cd sharp-bundle && npm ci --os=linux --cpu=arm64 sharp && cp -r node_modules ../dist/node_modules"
  }
}
